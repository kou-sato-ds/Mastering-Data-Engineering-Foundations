# =====================================================================
# テスト実行に必要な依存の固定 (CI と ローカルで同一環境を再現する)
#
# バージョンを固定する理由:
#   「ローカルでは緑だが CI では赤」という再現不能な事故を防ぐため。
#   apache-beam のマイナー更新で TestPipeline の挙動が変わる可能性がある。
#
# protobuf を明示固定する理由:
#   apache-beam は protobuf<7 を要求するが、新しい google-cloud-* は 7.x を
#   引き込もうとする。CI の一括解決ではこの矛盾を pip が解けず失敗するため、
#   両者が同時に満たせる 6.x 系を明示的に指定して解を与える。
# =====================================================================
apache-beam==2.75.0
pytest>=8.0.0
protobuf>=6.31.1,<7.0.0

# 👉 #71 STAGE 5 (クライアント生成位置の検証) を skip させないために必須
google-cloud-monitoring>=2.0.0
google-cloud-logging>=3.0.0
google-cloud-pubsub>=2.0.0

# 👉 #73 STAGE 6 (コストガード閾値の検証) を skip させないために必須
google-cloud-bigquery>=3.0.0

# 👉 #76 (IAM最小権限の定数検証) を skip させないために必須
google-cloud-iam>=2.0.0

Add-Content -Path requirements-test.txt -Value "google-cloud-resource-manager>=1.0.0"
Get-Content requirements-test.txt
